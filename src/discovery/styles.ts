// Le libellé mono de la section Discovery. Même rôle typographique que le
// reste du site, avec `leading-none` en plus : ces libellés sont posés dans
// des cellules serrées où l'interligne par défaut décalerait la ligne de base.
export const labelBase =
  'font-mono text-xs uppercase tracking-widest leading-none';

export const cellBase = 'relative min-h-0 min-w-0 overflow-hidden';
