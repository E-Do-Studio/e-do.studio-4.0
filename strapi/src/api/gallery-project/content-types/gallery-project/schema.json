{
  "kind": "collectionType",
  "collectionName": "gallery_projects",
  "info": {
    "singularName": "gallery-project",
    "pluralName": "gallery-projects",
    "displayName": "Galerie · Projet",
    "description": "Projet shooting affiché dans la galerie publique du site.",
    "icon": "picture"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "minLength": 2,
      "maxLength": 120,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Titre du projet (ex: « Campagne SS26 — Saar Paris »)."
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true,
      "description": "URL du projet (auto-généré). Ne plus modifier après publication."
    },
    "stage": {
      "type": "enumeration",
      "enum": ["live", "eclipse", "horizontal", "vertical", "cyclorama"],
      "description": "Plateau utilisé pour ce projet. Liste fermée pour éviter les valeurs invalides."
    },
    "year": {
      "type": "string",
      "regex": "^\\d{4}$",
      "minLength": 4,
      "maxLength": 4,
      "description": "Année du projet (4 chiffres, ex: « 2026 »). Convertie depuis integer par la migration `2026.05.07T10.10.00.gallery-project-year-int-to-varchar.js` qui tourne au boot."
    },
    "rank": {
      "type": "integer",
      "configurable": false
    },
    "category": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::gallery-category.gallery-category",
      "description": "Catégorie principale du projet."
    },
    "brand": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::gallery-brand.gallery-brand",
      "description": "Marque cliente du projet."
    },
    "images": {
      "type": "media",
      "multiple": true,
      "allowedTypes": ["images"],
      "description": "Galerie d'images du projet (15 max recommandé). L'image 1 est utilisée comme couverture par défaut."
    },
    "media": {
      "type": "component",
      "repeatable": true,
      "component": "shared.media-item",
      "description": "Médias du projet (images + vidéos). Si renseigné, prioritaire sur le champ `images` historique. L'item 1 sert de couverture par défaut."
    }
  }
}
