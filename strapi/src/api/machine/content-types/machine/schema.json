{
  "kind": "collectionType",
  "collectionName": "machines",
  "info": {
    "singularName": "machine",
    "pluralName": "machines",
    "displayName": "Plateau · Machine",
    "description": "Machine / plateau de shooting (Live, Eclipse, Horizontal, Vertical…). Ordonnable par drag & drop dans la liste.",
    "icon": "cube"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "minLength": 2,
      "maxLength": 80,
      "description": "Nom de la machine (ex: « Eclipse »). Non localisé : les noms sont identiques FR/EN."
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true,
      "description": "Identifiant URL (auto-généré). Utilisé par le front dans les routes /plateau/{slug}."
    },
    "subtitle": {
      "type": "string",
      "maxLength": 120,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Accroche courte affichée sous le titre."
    },
    "description": {
      "type": "text",
      "required": true,
      "maxLength": 600,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Description de la machine (≤ 600 caractères)."
    },
    "specs": {
      "type": "component",
      "component": "shared.spec",
      "repeatable": true,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Caractéristiques techniques (label / valeur). Localisé : chaque locale a sa propre liste éditable."
    },
    "pricing": {
      "type": "string",
      "maxLength": 200,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "ATTENTION : actuellement parsé en regex côté front (`split(' · ')`). À refactorer en composant `pricing-row` (Phase 3). Format : « 1 heure / € 120 · Demi-journée / € 410 »."
    },
    "operatorPricing": {
      "type": "string",
      "maxLength": 200,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Tarification avec opérateur (même format que pricing). Optionnel."
    },
    "pricingRows": {
      "type": "component",
      "component": "shared.pricing-row",
      "repeatable": true,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Tarification structurée — remplace progressivement le champ `pricing` (texte parsé en regex). Tant que vide, le front continue de lire `pricing`. Localisé : libellés FR/EN indépendants."
    },
    "pricingDescription": {
      "type": "text",
      "maxLength": 600,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Note tarifaire affichée sous la grille des prix (ex: « Remise en blanc 110 € · Électricité 1,40 €/kWh »). Optionnel."
    },
    "usages": {
      "type": "component",
      "component": "shared.localized-item",
      "repeatable": true,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Cas d'usage du plateau (« Usages » sur la page front), présentés sous forme de liste. Localisé : items FR/EN indépendants. Optionnel — si vide, le front utilise un dictionnaire de secours par slug."
    },
    "operatorPricingRows": {
      "type": "component",
      "component": "shared.pricing-row",
      "repeatable": true,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Tarification opérateur structurée — remplace progressivement `operatorPricing`. Localisé : libellés FR/EN indépendants."
    },
    "machineImage": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images", "videos"],
      "description": "DÉPRÉCIÉ — ne plus remplir (EDO-249). La cover de la page Plateau est désormais la PREMIÈRE entrée de `media`. Conservé en fallback pour les anciennes fiches : si renseigné et absent de `media`, il est utilisé comme première image du carrousel cover."
    },
    "media": {
      "type": "media",
      "multiple": true,
      "required": true,
      "allowedTypes": ["images", "videos"],
      "description": "Médias du plateau (images et/ou vidéos), ordonnés. La PREMIÈRE entrée sert de cover sur la page Plateau ; les flèches dans la cover font défiler le reste de la liste. Les vidéos sont jouées en autoplay/muted/loop. Migration : `2026.05.25T21.00.00.machine-unify-media.js`."
    },
    "seo": {
      "type": "component",
      "component": "shared.seo-meta",
      "repeatable": false,
      "description": "Métadonnées SEO de la page plateau. Si vides, le titre/description et le premier média sont utilisés en fallback."
    }
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  }
}
