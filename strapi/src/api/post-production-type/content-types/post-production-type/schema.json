{
  "kind": "collectionType",
  "collectionName": "post_production_types",
  "info": {
    "singularName": "post-production-type",
    "pluralName": "post-production-types",
    "displayName": "Post-prod · Type",
    "description": "Prestation de post-production (retouche, ghost, détourage, etc.). Ordonnable par drag & drop.",
    "icon": "scissors"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "minLength": 2,
      "maxLength": 80,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Nom de la prestation (ex: « On model », « Ghost »)."
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true,
      "description": "Identifiant URL (auto-généré)."
    },
    "description": {
      "type": "text",
      "required": true,
      "maxLength": 600,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Description de la prestation (accroche éditoriale)."
    },
    "priceRows": {
      "type": "component",
      "component": "shared.pricing-row",
      "repeatable": true,
      "description": "Tarification structurée de la prestation. Localisable via le composant lui-même."
    },
    "includes": {
      "type": "component",
      "component": "shared.localized-item",
      "repeatable": true,
      "description": "Ce qui est inclus dans la prestation (liste à puces)."
    },
    "media": {
      "type": "media",
      "multiple": true,
      "required": true,
      "allowedTypes": ["images", "videos"],
      "description": "Médias de démonstration de la prestation : exactement 6 entrées, images ou vidéos. Validé par lifecycles.ts (refus si nb ≠ 6). Remplace les anciens champs `image` (cover unique) et `gallery` (exemples). Migrations: `2026.05.25T14.00.00.post-production-type-unify-media.js` (rename des liens morph existants vers ce champ)."
    },
    "seo": {
      "type": "component",
      "component": "shared.seo-meta",
      "repeatable": false,
      "description": "Métadonnées SEO de la page post-production."
    }
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  }
}
