{
  "kind": "collectionType",
  "collectionName": "gallery_brands",
  "info": {
    "singularName": "gallery-brand",
    "pluralName": "gallery-brands",
    "displayName": "Galerie · Marque",
    "description": "Marque cliente affichée dans la galerie et le marquee de la home.",
    "icon": "tag"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "minLength": 1,
      "maxLength": 80,
      "description": "Nom de la marque tel qu'affiché côté site. Identique dans toutes les langues (un nom de marque ne se traduit pas — EDO-159)."
    },
    "url": {
      "type": "string",
      "regex": "^https?://.+",
      "description": "Site web de la marque (https:// recommandé)."
    },
    "logo": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"],
      "description": "Logo de la marque (SVG ou PNG transparent recommandé)."
    },
    "seo": {
      "type": "component",
      "component": "shared.seo-meta",
      "repeatable": false,
      "description": "Métadonnées SEO si la marque a une page dédiée côté site."
    }
  }
}
