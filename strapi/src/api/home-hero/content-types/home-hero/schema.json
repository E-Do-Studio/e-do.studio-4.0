{
  "kind": "singleType",
  "collectionName": "home_heroes",
  "info": {
    "singularName": "home-hero",
    "pluralName": "home-heroes",
    "displayName": "Home · Showreel",
    "description": "Vidéo en boucle + image poster affichées dans la cellule showreel de la home.",
    "icon": "play"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "video": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["videos"],
      "description": "Vidéo en boucle (mp4 recommandé, muet, ~10–30s). Le front affiche un fallback statique tant que Strapi renvoie 404/null."
    },
    "poster": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["images"],
      "description": "Image poster affichée avant la lecture de la vidéo. Le front affiche un fallback statique tant que Strapi renvoie 404/null."
    },
    "posterAlt": {
      "type": "string",
      "maxLength": 200,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Texte alternatif du poster (accessibilité)."
    }
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  }
}
