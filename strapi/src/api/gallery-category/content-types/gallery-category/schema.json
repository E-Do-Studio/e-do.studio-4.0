{
  "kind": "collectionType",
  "collectionName": "gallery_categories",
  "info": {
    "singularName": "gallery-category",
    "pluralName": "gallery-categories",
    "displayName": "Galerie · Catégorie",
    "description": "Catégorie de projet affichée comme filtre dans la galerie (Prêt-à-porter, Bijoux, etc.).",
    "icon": "layer"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "minLength": 2,
      "maxLength": 60,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      },
      "description": "Nom de la catégorie côté site (ex: « Prêt-à-porter »)."
    },
    "slug": {
      "type": "uid",
      "targetField": "name",
      "required": true,
      "description": "Identifiant URL (auto-généré). Utilisé par le front comme clé de filtre."
    },
    "group": {
      "type": "enumeration",
      "enum": [
        "live",
        "eclipse",
        "ghost",
        "pique",
        "horizontal",
        "vertical",
        "packshot",
        "cyclorama",
        "post-prod"
      ],
      "description": "DEPRECATED — couplage incorrect catégorie/plateau. À supprimer ou remplacer par une relation vers `machine` (Phase 3). Aujourd'hui jamais lu côté front."
    },
    "rank": {
      "type": "integer",
      "configurable": false
    },
    "seo": {
      "type": "component",
      "component": "shared.seo-meta",
      "repeatable": false,
      "description": "Métadonnées SEO si la catégorie a une page dédiée côté site."
    }
  }
}
